---
phase: 02-trading-analyst-crew
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: ["tools/ta_tools.py", "crews/ta_crew.py", "tests/test_ta_crew.py"]
autonomous: true
---

<objective>
## Goal
Build Trading Analyst crew — validates every trade against PM strategy spec. Catches violations before execution. Routes compliance report to PM and execution ledger to AM.

## Output
- `tools/ta_tools.py` — 7 deterministic tools (spec validation, slippage check, duplicate detection, compliance report, execution ledger)
- `crews/ta_crew.py` — 2-agent CrewAI crew (TradeValidator, ComplianceReporter)
- `tests/test_ta_crew.py` — 15 passing tests
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md
@ralph/prds/ta_prd.yaml
@crew_structure.py
@tools/om_tools.py — Pattern reference (env-var mocking, evidence strings)
@crews/om_crew.py — Pattern reference (hierarchical crew, @tool wrappers)
@tests/test_om_crew.py — Pattern reference (monkeypatch, test naming)
</context>

<acceptance_criteria>

## AC-1: Strategy spec validation correct
```gherkin
Given a PM strategy spec {type: "IRON_FLY", strikes: [24100, 24200, 24500, 24800], wings: 300, lots: 1, sl: 3500, tsl: 250}
And an actual trade that matches exactly
When validate_trade() is called
Then returns {valid: True, violations: []}
```

## AC-2: Violations detected
```gherkin
Given a spec with lot=1 and actual trade with lot=2
When validate_trade() is called
Then returns {valid: False, violations: [{field: "lots", expected: 1, actual: 2, severity: "CRITICAL"}]}
```

## AC-3: Slippage detection
```gherkin
Given spec strike=24500 and actual fill=24535 (>50 point slippage)
When check_slippage() is called
Then returns {ok: False, slippage: 35, evidence: "35 pts above spec"}
```

## AC-4: 15 tests pass
```gherkin
Given pytest runs tests/test_ta_crew.py
Then 15/15 pass — engine-only, no LLM
```

</acceptance_criteria>
