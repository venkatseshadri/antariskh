## [S0.1] Reconcile ABOUTME.md parameter values

**Epic:** E0 (Truth & Reconciliation) · **Priority:** P0 · **Repo:** root (`/home/trading_ceo`) · **Issue:** #1
**Workflow:** `ds:ready → ds:done → claude:review → chairman:approve` (see `antariksh/docs/PRD_GOLIVE.md` §5)

### Goal
`ABOUTME.md` must state the **same** SL/TP numbers as the code's single source of truth,
`antariksh/risk_config.py`. Today it contradicts itself and the code.

### Context / why
- `ABOUTME.md` §2 ("Why I Believe This Works") says **SL ₹2,500 / profit lock ₹5,000**.
- `ABOUTME.md` §4 (Two-Message example) says **SL ₹3,500 / TP ₹1,000**.
- Canonical values in code: `antariksh/risk_config.py:56` `daily_sl = 3_500.00`, and the TP
  target ₹1,000 (PRD §1 reconciliation table). **₹3,500 / ₹1,000 wins.**
- The ₹5,000 "profit lock" is a stale concept; remove it or relabel it explicitly.

### Files to change (verified 2026-06-13)
- `ABOUTME.md` — §2 paragraph 1 ("…SL Rs 2,500 per trade (0.5% of total capital). Profit
  lock at +Rs 5,000.") → SL ₹3,500 / TP ₹1,000, with a note "values enforced by
  `risk_config.py`, not this doc." Doc-only; **no code change.**
- Reference (read, do not edit): `antariksh/risk_config.py:56` (daily_sl), `:62`
  (portfolio_sl 4500), `:65` (max_30day_dd 30000), `:111` (nifty_lot_size 65).

### Read first
- `antariksh/docs/PRD_GOLIVE.md` §1 (reconciliation table — the resolved values).

### Dependencies
- Depends-on: none. Blocks: nothing (independent). Sibling P0: #2, #3.

### Reproduce current state
```bash
cd /home/trading_ceo
grep -nE '2,?500|5,?000|3,?500|1,?000' ABOUTME.md     # see the contradictions
grep -n 'daily_sl' antariksh/risk_config.py            # the canonical 3500
```

### Implementation steps
1. Edit `ABOUTME.md` §2: replace "SL Rs 2,500 … Profit lock at +Rs 5,000" with
   "SL ₹3,500 per trade, TP ₹1,000 (enforced by `risk_config.py`)."
2. Scan the rest of the doc for any other ₹2,500/₹5,000 SL/TP usage; align or remove.

### Verify
```bash
cd /home/trading_ceo
# no stray SL/TP values remain:
grep -nE '2,?500|5,?000' ABOUTME.md   # must return no SL/TP context lines
```
No tests/PORCUPINE needed — documentation-only change.

### Done when
- [ ] ABOUTME §2 shows ₹3,500 / ₹1,000 + the "enforced by risk_config.py" note.
- [ ] `grep -nE '2,?500|5,?000' ABOUTME.md` shows no SL/TP usages.
- [ ] Rubric self-check (PRD §3): surgical, doc-only, no code touched.
