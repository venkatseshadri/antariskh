# PORCUPINE runtime state & sandbox artifacts — never commit (code only)
run_*/
logs/
__pycache__/
.autobuild.lock
.autobuild_paused
.autobuild_attempts
.porcupine_status.json
.last_p3_root
# NOTE: .bug*_fixed markers ARE committed (intentionally not ignored) — they
# durably record that a human-gated live-code fix landed, surviving context loss.
